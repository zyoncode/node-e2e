import { errors } from '@appium/base-driver';

const commands = {};

commands._validateOrUpdateWinInfo = function () {};

commands.findElOrEls = function findElOrEls(
  strategy,
  selector,
  mult,
  context,
) {};

export { commands };
export default commands;
